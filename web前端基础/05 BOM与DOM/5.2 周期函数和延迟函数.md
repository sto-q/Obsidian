![[Pasted image 20260531165045.png]]
```
<body>
<div id="time"></div>
</body>
<script="text/javascript">

%% setInterval(function(){
let now=new Date()://创建一个当前系统时间的对象
let year=now.getFullYear();//获取年份
let month=now.getMonth()+1;获取月份,0-11
let date=now.getDate();获取日期是当前月份第几天
let hour=now.getHours();获取小时
let minute=now.getMinutes();获取分钟
let second=now.getSeconds();//获取秒
let time=year+"-"+zerfill(month)+"-
"+zerfill(date,2)+""+zerfill(hour,2)+":"+zerfill(minute,2)+":"+zerfill(scend,2);
let div=document.getElementById("time");
div.textContext=time;

},1000); %%

lei count=0;
function showTime(){
let now=new Date()://创建一个当前系统时间的对象
let year=now.getFullYear();//获取年份
let month=now.getMonth()+1;获取月份,0-11
let date=now.getDate();获取日期是当前月份第几天
let hour=now.getHours();获取小时
let minute=now.getMinutes();获取分钟
let second=now.getSeconds();//获取秒
let time=year+"-"+zerfill(month)+"-
"+zerfill(date,2)+""+zerfill(hour,2)+":"+zerfill(minute,2)+":"+zerfill(scend,2);
let div=document.getElementById("time");
div.textContext=time;
count++;//count为10时周期函数停止
%% 
	if(count==10){
	clearInterval(t);//清理给定的周期函数
	} 
%%
}
//let t=setInterval('showTime()',1000);

//setInterval(showTime,1000);//每隔1秒调用showTime函数
//setInterval('showTime()',1000);//用''包起来表示调用函数 如果第一个参数传递的是字符串必须是函数的调用
let s=setTimeout(shoeTime,3000);//在3秒后执行一函数
clearTimeout(s);

function zerfill()(num,targetLen){
	let str=num+"";
		while(str,length<targetLen){
			str="0"+str;
		}
	return str;
}
</script>
```

