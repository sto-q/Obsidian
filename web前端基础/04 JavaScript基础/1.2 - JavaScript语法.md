![[Pasted image 20260529131129.png]]
### 2变量
![[Pasted image 20260529131213.png]]
![[Pasted image 20260529131813.png]]
var没有类型之分看你给的是什么
```
{
var number1=1;
let number2=2;
}
//console表示控制台log表示日志记录
console.log(number1);
//无法访问number2所以说ver定义的是全局变量let定义的变量只在括号中生效
console.log(number2);
```
### 3字符串
```
let s1="字符串";
let s2='字符串';单引号与双引号括起来的都是字符串
```
![[Pasted image 20260529132018.png]]
```
let index=s1.indexOf("符",2);
	//indexOf方法带有两个参数：第一个表示要查找的字符串，第二个表示从字符串中给定的位置开始查找，如果没有传递第二个参数，则默认为0；如果为找到返回-1；
let c=s1.charAt(0);返回的是一个字符串
console.log(typedof c);typedof表示....的类型
let sub=s1.substring(1,3);[1,3)
let arr=s1.split("");//给定的字符串按照空白字符串拆分
console.log(arr)//["字"符"串"];
let newstr=s1.replace("字","啊");//啊符串-只能替换第一个
```
#### 3数组
![[Pasted image 20260529133246.png]]
元素赋值与Java一样
```
number[2]=22;
```
![[Pasted image 20260529133313.png]]
```
let srt=s1.join(%)//字%符%串
```
![[Pasted image 20260529133750.png]]
对象的属性可以直接赋值