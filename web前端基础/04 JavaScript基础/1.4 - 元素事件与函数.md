![[Pasted image 20260529145644.png]]
```
	<script type="text/javascript">
	alert("这是提示信息");
	-----------------
	确认对话框会有一个返回值，该值表示用户是否进行确认
	let result=confirm("确定要删除吗")
	console.log(result);//确认为true取消为false
	let input= prompt("请输入一个数字")
	console.log(input)//输入对话框有一个返回值，该值为输入的信息，取消了就是null，不输入点确定为空字符串
	</script>
	
```
![[Pasted image 20260529150415.png|252]]
![[Pasted image 20260529150355.png|255]]
![[Pasted image 20260529150345.png|263]]
![[Pasted image 20260529150620.png]]
```
//javascript中的parseInt函数可以将任意数字开头的任意字符串转换为整数
	<scrpit type="text/javascript>
	let a=parseInt("12a3");
	console.log(a);
	-----------------
//javascript中的parseFloat函数可以将任意数字以及'.'开头的任意字符串转换为浮点数
	let b=parseFloat(".123aaa");
	console.log(b);
	-------------------
	let c=inNaN("23");
	console.log(c);
	</script>
```

![[Pasted image 20260529150627.png]]
```
<script type="text/javascript>
console.log(Math.ceil(0.225));//1只看小数点后一位
console.log(Math.floor(0.999));//0只看小数点后一位
console.log(Math.ads(-1));//1绝对值
console.log(Math.round(2.5));//3
console.log(Math.random());
</script>
```
###  </red> 自定义函数



```
function 函数名(参数1，参数2..参数n){
//javaScript
return 返回值 ;//要返回值就return，不要就不加
}
//函数调用
函数名(参数值1，参数值2...参数值n);
```
```
在JavaScript中一个函数的返回值可以是一个函数
function calculate(a,b,c){
	let result=a*b;
	return function (d){
	rerturn  result+c*d;
	}
}
//此时注意的是calculate执行后返回的是一个函数，也就是说在JavaScript中变量可以存储一个函数，这个情况，我们把这个变量当作函数使用即可
let s=calculate(1,2,3);//2
let num=s(4);//再次调用函数得到结果14
//闭包
let result=calculate(1,2,3)(4);//函数调用
console.log(result);
--------------
//闭包
(function calculate(a,b,c){
	let result=a*b;
	return function (d){
	rerturn  result+c*d;
	}
})(1,2,3)(4);
```
#### 4元素事件与函数
![[Pasted image 20260529153322.png]]
```
<body>
<div>
<input type="text" id="username"self="abc"value="admin">
</div>

<div>
//onclick表示开启所在元素的点击事件
<input type="button" value="点击" onclick="showValue()">
</div>
</body>


<script type="text/javascript">
function showValue(){
//document 表示一个文档对象，是JavaScript内置对象，不需要new 直接可以使用，主要提供的功能就是操作页面元素比如创建，查找元素

let input=document.getElementById("username");//查找Id为username的
console.log(input);//打印<script type="text/javascript">
//凡是写在标签上的都是属于标签的属性
console.log(input.value)
//自定义属性需要使用方法来获取值
console.log(input.getAttribute("self"))
}
</script>
```
```
------------------------------------------------------
let input=document.getElementById("username");
//给ID为username的标签添加一个获得焦点事件
input.onfocus(){
	console.log("input输入框获得了焦点")
}
//给ID为username的标签添加一个失去焦点事件
input.onblur=function(){
	if(input.value==""){
		console.log("input 输入框没有进行任何输入")
	}else{
		console.log(input.value);
	}
}
```
```
--------------------------------------------------------------
input.onkeyup是键盘按下后松开
input.onkeypress是鼠标按下
input.onkeydown=function(e){//e表示event，表示一个事件对象
//console.log(e);
	if(e.code==='Enter'){//主要用于用户登录界面用户输入完毕后按Enter进行登录
		console.log("你按下了Enter")
	}
}
```
```
<div id="box"></div>
<script type="text/javascript">
let box=document.getElementById("box");
//鼠标悬浮在元素上
	box.onmouseover=function(){
		box.style.backgroundColor="greed";
	}
//鼠标进入
	box.onmouseenter=function(){
		box.style.backgroundColor="greed";
	}
鼠标离开
	box.onmouseout=function(){
		box.style.backgroundColor="red";
	}
```
```
----------------------------------------------------------------
<body>
<input type="text" id="text">

<select id="sex">
<option>男</option>
<option>女</option>
<option>其它</option>
</select>

<input type="file" id="file">
</body>


<script type="text/javascript">
let text=document.getElementById("text");
//在输入内容时按下enter键或者元素失去焦点才会触发
	text.onchange=function(){
		console.log("change"+text.value)
	}
//在输入内容发生改变时立即触发与是否按下enter无关
	text.oninout=function(){
		console.log("input"text.value)
	}
	//适用于下拉模块
let sex=document.getElementById("sex");
	sex.onchange=function(){
		console.log("change"sex.value);
	}
	sex.oninput=function(){
		console.log("input"sex.value);
	}
	//适用于文件模块
let file=document.getElementById("file");
	file.onchange=function(){
		console.log("change"file.fils);
	}
	file.oninput=function(){
		console.log("input"file.fils);
	}
</script>
```