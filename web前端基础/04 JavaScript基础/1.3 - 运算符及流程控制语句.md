![[Pasted image 20260529133930.png]]
```
!== === 表示要检测内容相同的同时还有检查数据类型是否相同
在JavaScript中两个整数相除可能得到浮点数
其余的与Java中一样
```
#### 流程控制语句
if语句
```
if(){

}else{
}
```
switch语句
```
switch(){
case 常量1:break;
case 常量2:break:
....
default:
}
```
循环语句
```
for(循环变量初始化;循环条件;更新){
}
---
while(循环条件){
}
---
do{
}while(循环条件)
---
for(let 变量名 in对象或数组){
}
//循环语句中的break和continue 
```
```
let arr[1,2,3,4,5,6,7];
for(let AA in arr){
console.log(arr[AA])
}//1,2,3,4,5,6,7AA相当于下标
------------------------
let stu{
name:,
sex:,
age:18,
score:86
};
for(let AA in stu){
console.log(stu[AA])
}//遍历不能用.因为在变化
对象属性除了用[]外还可以用.来取值
console.log(stu['name']);
console.log(stu.name);
```