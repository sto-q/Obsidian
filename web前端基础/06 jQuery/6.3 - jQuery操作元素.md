![[Pasted image 20260601212321.png]]
```
<style>
    .error{
        color: red;
        height: 30px;
        line-height: 30px;
    }
    .info{
        color: blue;
    }
</style>
<body>
    <div class="error"></div>
    <div class="info"></div>
</body>
<script type="text/javascript" src="js\jquery-4.0.0.js"></script>
<script type="text/javascript">
    $(function(){
// $(".error").text("账号或密码错误");
let elem=$(".error");//JQuery对象是一个集合
//集合可以通过下标访问某个位置的元素
//elem[0].textContent="账号或密码错误";
//集合可以通过get方法传递一个下标去获取对应的位置的元素
//elem.get(0).textContent="账号或密码错误";
//不论是通过下标直接获取还是通过下标用get方法获取，得到的都是一个DOM对象，DOM对象可以直接使用属性
elem.get(0).innerText="账号或密码错误";
//$(".info").text("<h1>显示1级标签</h1>");
$(".info").html("<h1>显示1级标签</h1>");
console.log($(".error").text());
console.log($(".info").html());
    });
</script>
```
![[Pasted image 20260601214850.png]]
```
<body>

    <input type="text" value="123">

    <div id="content" data-info="这是测试内容"></div>

</body>

<script type="text/javascript" src="js\jquery-4.0.0.js"></script>

<script type="text/javascript">

    $(function(){

        //获取input上type属性值

        let tye=$("input").attr("type");

        console.log(tye);

        //获取input上value属性值

        let valu=$("input").attr("value");

        console.log(valu);

        valu=$("input").val();

        console.log(valu);

        $("input").attr("value","345896");

        $("input").val("346");

        valu=$("input").val();//val只能获取value属性的值

        console.log(valu);

        //attr还可以获取到自定义的属性值

        let dataInfo=$("#content").attr("data-info");

        console.log(dataInfo);

        dataInfo=$("#content").attr("data-info","这是修改的内容");

        console.log(dataInfo);

        //removeAttr方法可以移除元素的属性包括自定义的

        $("#content").removeAttr("data-info");

    })

</script>
```
![[Pasted image 20260601220925.png]]
```
<body>

    <div class="info"></div>

</body>

<script type="text/javascript" src="js/jquery-4.0.0.js"></script>

<script type="text/javascript">
    $(function(){  
        let info=$(".info")
        info.width("200px");
        info.height("200px");
        info.css("background-color", "red");
        console.log(info.width()+"x"+info.height());
    });
</script>
```

![[Pasted image 20260601221352.png]]
```
<body>

    <div class="info">这是测试文字</div>

</body>

<script type="text/javascript" src="js/jquery-4.0.0.js"></script>

<script type="text/javascript">

    $(function(){  

        let info=$(".info")

        info.width("200px");

        info.height("200px");

        console.log(info.width()+"x"+info.height());

        //如果css样式属性名中有‘-’符号，那么可以使用驼峰命名法将每个单词进行组装作为css样式属性名

        info.css("backgroundColor", "red");

        // info.css("background-color", "red");

        console.log(info.css("background-color"));

        info.css({

            "font-size":"30px",

            "border":"1px solid black",

        });

  

    });

</script>
```
![[Pasted image 20260601223414.png]]
```

```