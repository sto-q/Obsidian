![[Pasted image 20260601225634.png|390]]
![[Pasted image 20260601225657.png|387]]
```
<body>
    <input type="button"value="查询" id="btn">
    <table>
        <thead>
            <tr>
                <td>姓名</td>
                <td>性别</td>
                <td>年龄</td>
               <td>操作</td>
            </tr>
        </thead>
        <tbody>
            <!-- <tr>
                <td>张三</td>
                <td>男</td>
                <td>18</td>
                <td>
                    <a href="javascript:void(0)">修改</a>
                    <a href="javascript:void(0)">删除</a>
                </td>
            </tr> -->
        </tbody>
    </table>
</body>
<script type="text/javascript" src="js/jquery-4.0.0.js"></script>
<script type="text/javascript">
    $(function(){
        //为ID为btn添加点击事件
        // $("#btn").on("click",function(){
        //      alert("你点击的事件");
        // });
        $("#btn").click(function(){
           let stus=[{
            name:"张三",
            sex:"男",
            age:"18",
           },{
            name:"张三",
            sex:"男",
            age:"18",
           }];
           let html=" ";
           for(let i=0;i<stus.length;i++){
            html+="<tr>";
                html+="<td>"+stus[i].name+"</td>";
                html+="<td>"+stus[i].sex+"</td>";
                html+="<td>"+stus[i].age+"</td>";
                html+='<td><a href="javascript:void(0)" class="update">修改</a><a href="javascript:void(0)" class="delete">删除</a></td>';
            html+="</tr>"
           }
           $("tbody").html(html);
        });
        //为tbody的使用类样式update元素添加点击事件
        $("tbody").on("click",".update",function(){
            alert("你点击了修改按钮");
        });
         //为tbody的使用类样式update元素添加点击事件
        $("tbody").on("click",".delete",function(){
            let result=confirm("确定要删除这条信息吗")
            if(result){
                console.log("删除了信息")
            };
        });
    })
</script>
```