![[Pasted image 20260602124943.png]]
```

<body>

    <div class="text"></div>

</body>

<script type="text/javascript" src="js\jquery-4.0.0.js"></script>

<script type="text/javascript">

    $(function(){

        //通过元素ID获取一个DOM对象

        let dom=document.getElementById("text");

        $(dom);//这是就是一个JQuery获取节点的对象

        $("#text");

  

        $("<tr></tr>");//这就是创建一个元素节点

    })

</script>
```
![[Pasted image 20260602125655.png]]
```
<script type="text/javascript">

    $(function(){


        //将给定的内容追加到JQ选择器选择的元素内容的末尾

        // $("#tex").append($("<input type='text'>"));

        // $("#tex").append($("<input type='text' id='tex'>"));

  

        //将选择器中的内容追加到给定的选择器定位的元素的内容的末尾

        // $("<input type='text'>").appendTo($("#tex"));

  

        //将给定的内容追加到JQ选择器选择的元素内容的最前面

        // $("#tex").prepend($("<input type='text'>"));

        // $("#tex").prepend($("<input type='text' id='tex'>"));

  

        //将选择器中的内容追加到给定的选择器定位的元素的内容的最前面

        //$("<input type='text'>").prependTo($("#tex"));

        //$("<input type='text' id='tex'>").prependTo($("#tex"));

  

        // //将给定的内容添加到给定的选择器定位的元素的后面，与定位的元素同级

        // $("#tex").after($("<input type='text'>"));

        // //将选择器中的内容添加到给定的选择器定位的元素的后面，与该定位的元素同级

        // $("<input type='text'>").insertAfter($("#tex"));

  

        //将给定的内容添加到给定的选择器定位的元素的前面，与定位的元素同级

        $("#tex").before($("<input type='text'>"));

        //将选择器中的内容添加到给定的选择器定位的元素的前面，与该定位的元素同级

        $("<input type='text'>").insertBefore($("#tex"));

  

    });

</script>
```
![[Pasted image 20260602132331.png]]
```
<body>

    <div id="content"></div>

</body>

<script type="text/javascript" src="js\jquery-4.0.0.js"></script>

<script type="text/javascript">

    $(function(){

        //使用给定选择器中的内容替换选择器选中的内容

        $("#content").replaceWith($("<ul><li>one</lI></ul>"));

    })

</script>
```
![[Pasted image 20260602132715.png]]
```
<body>

    <div id="content">ssss</div>

    <input type="text" value="asss">

</body>

<script type="text/javascript" src="js\jquery-4.0.0.js"></script>

<script type="text/javascript">

    $(function(){

        //使用给定选择器中的内容替换选择器选中的内容

        // $("#content").replaceWith($("<ul><li>one</lI></ul>"));

        $("#content").empty();//清空选择器定位的元素中所有的内容，但节点还在

        $("#content").remove();//移除选择器定位的内容

    })

</script>
```
![[Pasted image 20260602133334.png]]
![[Pasted image 20260602133402.png|389]]
![[Pasted image 20260602135139.png]]
```
<body>

    <div class="content">

        <ul class="content">

            <li class="first">1</li>

            <li>2</li>

            <li>3</li>

            <li>4</li>

        </ul>

    </div>

</body>

<script type="text/javascript" src="js\jquery-4.0.0.js"></script>

<script type="text/javascript">

    $(function(){

        //定位ul获取ul下一级所有子元素

        let childer=$(".content > ul").children();

        console.log(childer);

  

        //获取ul上一级父元素

        // let parent=$(".content>ul").parent();

        //  console.log(parent);

  

        //查找距离该元素最近的父元素

        //  let node=$(".first").closest("ul");

        //   console.log(node);

  

        //查找与JQ选择器定位的元素的同级的下一个元素

        console.log($(".first").next());

        console.log($(".first").prev());

  

        //获取与JQ选择器定位的元素的同级的所有元素

        console.log($(".first").siblings());

  

        // $("li").first();

        console.log($("li").first());

        console.log($("li").last());

  

        //在jQ选择器定位的元素中根据给定的选择器查找元素    

         console.log($("div.content").find("#first"));

  

         childer.each(function(index,e){

            console.log(index+"=>"+e);

            console.log(index);

            console.log(e);

         })

    })

</script>
```