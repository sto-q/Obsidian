![[Pasted image 20260601151940.png]]
```
$("form");//选中页面中所有的form表单

$("#username");//ID选择器，选中ID为username的元素

$(".form.group");//选中所有类样式为foem-group的元素

$("input[type=text]");//选中所有的文本输入框

$("input[name=username]")//选中名称为username的输入框

$("div,span");//并集选择器，选中所有的div,span元素

$("form input");//后代选择器，选中form下所有的span标签

$("div>span");//子代选择器，选中所有div标签下一级的所有span标签

  
  

$("div:first");//过滤选择器，选中所有div标签中的第一个标签

$("div:last");//过滤选择器，选中所有div标签中的最后一个标签

  
  

$("div:even");////过滤选择器，选中所有div标签中下标为偶数的标签

$("div:odd");//过滤选择器，选中所有div标签中下标为奇数的标签

  

$("div:eq(0)");//过滤选择器，选中所有div标签中下标为0的标签

//gt=greater than 大于

$("div:gt(0)");////过滤选择器，选中所有div标签中的下标大于0的标签

//lt=less than 小于

$("div:lt(0)");////过滤选择器，选中所有div标签中的下标小于0的标签

$("input:focus");//gt=greater than 大于

$("div:gt(0)");////过滤选择器，选中所有input标签中获取焦点的标签

  

$(document);//选中整个文档
```
### 2
![[Pasted image 20260601154646.png]]
![[Pasted image 20260601154848.png]]
