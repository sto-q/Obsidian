![[Pasted image 20260918164450.png]]



```java
@RunWith(SpringJUnit4ClassRunner.class)//测试模板类需要用到一个测试启动类  
@ContextConfiguration(classes = {SpringConfiguration.class})//在测试模板中也引入ioc环境  
public class testJunit {  
    @Autowired  
    AccountService accountService;  
    @Test  
    public void test1(){  
        Account account=accountService.findAllAccountById(1);  
        System.out.println(account);  
    }  
}
```