```java
//接收前端日期的函数  
@RequestMapping("getDate")  
public String getDate(@DateTimeFormat(pattern = "yyyy-mm-dd") Date date){  
    System.out.println(date);  
    return "success";  
}  


@RequestMapping("getDate2")  
public String getDate2(Date date){  
    System.out.println(date);  
    return "success";  
}
```

```jsp
<a href="${pageContext.request.contextPath}/demo1/getDate?date=2025-10-11">提交日期类型的数据</a>  
<a href="${pageContext.request.contextPath}/demo1/getDate2?date=2025-10-11">提交日期类型的数据</a>
```

```java
/**  
 * 自定义数据类转换器  
 */  
public class DateFormatConvert implements Converter<String,Date> {  
    //进行数据类型的传换  
    @Override  
    public Date convert(String source) {  
        try {  
            if(StringUtils.isEmpty(source)){  
                throw new NullPointerException("前端提交的参数不能为空");  
            }  
            SimpleDateFormat sdf=new SimpleDateFormat("yyyy-MM-dd");  
            Date date = sdf.parse(source);  
            return date;  
        } catch (ParseException e) {  
            e.printStackTrace();  
            throw new RuntimeException("输入的日期错误");  
        }  
    }  
}


```
```xml


<!--    配置数据类型转换器-->  
<bean id="conversionServiceFactoryBean" class="org.springframework.context.support.ConversionServiceFactoryBean">  
    <property name="converters">  
        <array>            
	        <bean class="com.lq.convert.DateFormatConvert"/>  
        </array>   
	 </property>
 </bean>
<!-- 开启处理器映射器 处理器适应器的自动配置   -->  
    <mvc:annotation-driven conversion-service="conversionServiceFactoryBean"/>
```