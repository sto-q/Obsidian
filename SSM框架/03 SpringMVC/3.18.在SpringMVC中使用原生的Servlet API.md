```java
//测试原生Servlet  
@RequestMapping("textServletAPI")  
public void textServletAPI(HttpServletRequest request , HttpServletResponse response){  
    try {  
        System.out.println(request.getParameter("username"));  
        System.out.println(request.getParameter("age"));  
        //重定向  
        response.sendRedirect("/pages/success.jsp");  
    } catch (IOException e) {  
        e.printStackTrace();  
    }  
}
```

```jsp
<a href="${pageContext.request.contextPath}/demo1/textServletAPI?username=eric$age=19">[测试原生ServletAPI]</a>

```